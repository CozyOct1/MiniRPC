# README 编写规范

MiniRPC 的主 README 采用统一项目模板，面向开发者说明真实代码、运行方式、目录结构、核心架构和当前限制。

文档原则：

- 所有功能描述必须对应当前仓库已有代码或明确标注为后续扩展。
- 路径统一使用仓库内相对路径。
- 图片资源统一放在 `images/`，GitHub Pages 资源放在 `docs/`。
- 性能数据必须来自可复现命令，未实测前只写建议压测项。
